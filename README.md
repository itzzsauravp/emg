# HOW TO USE

clone it somewhere

```bash
git clone https://github.com/itzzsauravp/emg
```

build and generate binary

```bash
npm run release
```

binary generated in `./dist/emg`

copy binary to bin

```bash
sudo cp ./dist/emg /usr/bin
```
